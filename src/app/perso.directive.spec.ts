import { PersoDirective } from './perso.directive';

describe('PersoDirective', () => {
  it('should create an instance', () => {
    const directive = new PersoDirective();
    expect(directive).toBeTruthy();
  });
});
